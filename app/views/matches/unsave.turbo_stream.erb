<%= turbo_stream.replace "save_button_#{@match.id}" do %>
  <%= turbo_frame_tag "save_button_#{@match.id}" do %>
    <%= link_to "Save this match",
                save_match_path(@match),
                data: { turbo_method: :patch },
                class: "btn-save" %>
  <% end %>
<% end %>
